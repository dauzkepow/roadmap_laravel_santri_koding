//Fungsi = Bentuk singkat dari if...else.

//Contoh Kode
let umur = 20;
let status = (umur >= 18) ? "Dewasa" : "Anak-anak"; 
console.log(status); //Dewasa


//Penjelasan per Baris Kode
/*
(umur >= 18) → kondisi.
Jika true → ambil "Dewasa".
Jika false → ambil "Anak-anak".
*/

//Latihan = Cek angka positif atau negatif
console.log("==============");
let a = 5;
let cek = (a > 0) ? "Positif" : "Negatif"; 
console.log(cek); //Positif