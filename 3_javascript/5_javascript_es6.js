//ES6+

/*
//Arrow Function
const add = (a,b) => a + b;

//Regular Function
function add(a,b) {
    return a + b;
}

//Object Destructuring
const user = {
    name : 'Daus',
    age : 30
};
const {name, age} = user;

//Array Destructuring
const colors = ['red', 'green'];
const [first, second] = colors;

//Template Literals = gunakan extension vscode = template string converter
const nama = "Bisma";
console.log(`Hello, ${nama}! Welcome to ES6`);

//Spread Operator
const nums = [1,2,3];
const allNums = [...nums, 4, 5];

//Rest Operator
function sum(...args) {
    return args.reduce((a,b) => a + b, 0)
}

//Default Parameters
function greet(name = 'Guest') {
    console.log(`Hello, ${name}`);
}

//Async / Await
async function fetchData() {
    const res await fetch ('https://api.example.com'); //wait for response
    const data = await res.json(); //parse JSON
    console.log(data); //output data
}

//Modules Import Export
//export.js
export const greet = () => console.log('Hi');

//import.js
import {greet} from './export.js';
greet();


*/